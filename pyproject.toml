[project]
name = "pala"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.12"
dependencies = [
    "numpy>=2.2.6",
    "pyyaml>=6.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.setuptools]
packages = ["pala"]
