[project]
name = "pala"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.12,<3.13"
dependencies = [
    "numpy>=1.26.4,<2",
    "pillow>=12.1.0",
    "pyyaml>=6.0.1",
    "smbus2>=0.6.0",
]

[dependency-groups]
dev = [
    "onnx>=1.20.1",
    "pytest>=9.0.2",
]

[tool.setuptools]
packages = ["pala"]
